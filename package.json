{
  "name": "omp-headroom",
  "version": "0.1.4",
  "description": "Headroom context-compression integration for OMP: proxy lifecycle, tool-output compression, session archive compaction with CCR retrieval, adaptive thresholds, and a live savings widget.",
  "author": "DarkPhilosophy",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DarkPhilosophy/omp-headroom.git"
  },
  "keywords": [
    "omp",
    "headroom",
    "llm",
    "context",
    "compression",
    "token",
    "proxy",
    "rocm",
    "cuda"
  ],
  "files": [
    "src",
    "scripts",
    "systemd",
    "README.md",
    ".github/CHANGELOG.md",
    "LICENSE"
  ],
  "exports": "./src/index.ts",
  "omp": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "pi": {
    "extensions": [
      "./dist-pi/pi-entry.js"
    ]
  },
  "scripts": {
    "build:pi": "bun scripts/build-pi-entry.ts",
    "postinstall": "bun run build:pi",
    "check": "biome check .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit",
    "test": "bun test tests/",
    "scan": "bun .scripts/scan-leaks.mjs",
    "verify": "bun run check && bun run typecheck && bun run test && bun run scan"
  },
  "peerDependencies": {
    "@oh-my-pi/pi-coding-agent": "^16.4.4 || ^17.0.0",
    "@earendil-works/pi-coding-agent": "^0.84.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "@earendil-works/pi-coding-agent": "0.84.4",
    "@oh-my-pi/pi-coding-agent": "17.0.6",
    "@types/bun": "^1.3.5",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "@opentelemetry/core": "2.8.0",
    "adm-zip": "0.6.0",
    "browserslist": "^4.28.7",
    "sharp": "^0.35.0"
  }
}
